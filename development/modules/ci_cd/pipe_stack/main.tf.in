module "code_pipe" {
  source                                       = "../../ci_cd/pipe"
  ci_cd_account_data                           = var.ci_cd_account_data
  pipe_map                                     = local.create_pipe_map
  {{ ci_cd.pipe_map_item() }}
  std_map                                      = var.std_map
}
