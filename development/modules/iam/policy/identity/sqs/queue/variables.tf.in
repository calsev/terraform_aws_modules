{{ name.var() }}

variable "policy_map" {
  type = map(object({
    {{ name.var_item() }}
    {{ iam.policy_var_item() }}
    queue_name              = string
  }))
}

{{ iam.policy_var_ar(access=["pull", "push"], append="queue") }}

{{ std.map() }}
