{{ name.var() }}

{{ iam.policy_var_ar(access=["read", "read_write"], append="secret") }}

variable "policy_map" {
  type = map(object({
    {{ name.var_item() }}
    {{ iam.policy_var_item() }}
    sm_secret_name          = optional(string)
    sm_secret_name_list     = optional(list(string), [])
    ssm_param_name          = optional(string)
    ssm_param_name_list     = optional(list(string), [])
  }))
}

{{ std.map() }}
