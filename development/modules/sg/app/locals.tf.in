locals {
  output_data = {
    (var.vpc_key_default) = merge(
      # Drop-in replacement for vpc_data_map
      var.vpc_data_map[var.vpc_key_default],
      module.sg_rule.data,
      module.sg.data[var.vpc_key_default],
      {
        security_group_id_map = merge(
          var.vpc_data_map[var.vpc_key_default].security_group_id_map,
          module.sg.data[var.vpc_key_default].security_group_id_map,
        )
      }
    )
  }
}
